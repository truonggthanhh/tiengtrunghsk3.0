import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.websitentpnewhsk30',
  appName: 'WEBSITE ÔN TẬP NEW HSK 3.0',
  webDir: 'dist'
};

export default config;
